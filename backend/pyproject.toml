[tool.ruff]
target-version = "py313"
extend-exclude = ["**/migrations/*.py"]

[tool.ruff.lint]
# Primera barrera sin imponer una reescritura estilística a todo el legado:
# errores de sintaxis, nombres indefinidos e imports objetivamente inválidos.
select = ["E9", "F63", "F7", "F82"]
