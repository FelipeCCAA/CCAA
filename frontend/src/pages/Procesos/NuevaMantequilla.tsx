import { useEffect, useState } from "react";
import { X } from "lucide-react";

import { crearMantequillaGuiada, obtenerOpcionesAltaMantequilla, type CorridaMantequilla, type OpcionesAltaMantequilla } from "../../services/procesos.service";

const campo = "mt-1 w-full rounded-xl border border-slate-300 bg-white px-3 py-2.5 text-sm";
const errorDe = (e: unknown) => { const d = (e as { response?: { data?: unknown } }).response?.data; return d && typeof d === "object" ? Object.values(d as Record<string, unknown>).flat().map(String).join(" ") : "No se pudo crear la corrida."; };

export default function NuevaMantequilla({ onCerrar, onCreada }: { onCerrar: () => void; onCreada: (corrida: CorridaMantequilla) => void }) {
  const [opciones, setOpciones] = useState<OpcionesAltaMantequilla | null>(null);
  const [datos, setDatos] = useState({ orden: "", crema: "", equipo: "", codigo: "", suero: "", kg: "" });
  const [error, setError] = useState(""); const [guardando, setGuardando] = useState(false);
  useEffect(() => { void obtenerOpcionesAltaMantequilla().then(setOpciones).catch(() => setError("No se pudieron cargar las opciones de mantequilla.")); }, []);
  const crema = opciones?.cremas.find((item) => item.id === Number(datos.crema));
  const guardar = async (e: React.FormEvent) => { e.preventDefault(); setGuardando(true); setError(""); try { onCreada(await crearMantequillaGuiada({ orden: Number(datos.orden), lote_crema: Number(datos.crema), equipo: Number(datos.equipo), codigo_lote_mantequilla: datos.codigo, ...(datos.suero ? { lote_suero: Number(datos.suero) } : {}), kg_crema: Number(datos.kg) })); } catch (err) { setError(errorDe(err)); } finally { setGuardando(false); } };
  return <div className="fixed inset-0 z-[80] flex items-start justify-center overflow-y-auto bg-slate-950/45 p-4"><form onSubmit={guardar} className="my-8 w-full max-w-2xl rounded-2xl bg-white p-6 shadow-xl"><div className="flex justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wide text-amber-700">Alta guiada</p><h2 className="mt-1 text-xl font-bold">Nueva corrida de mantequilla</h2><p className="mt-2 text-sm text-slate-600">Crema con saldo → línea de mantequilla → nuevo lote trazable.</p></div><button type="button" onClick={onCerrar} className="h-fit rounded-lg p-2 hover:bg-slate-100"><X className="h-5 w-5" /></button></div>{!opciones && !error ? <p className="mt-6 text-sm text-slate-500">Cargando opciones…</p> : <div className="mt-6 grid gap-4 sm:grid-cols-2"><Selector texto="Orden de mantequilla" valor={datos.orden} cambiar={(v) => setDatos({ ...datos, orden: v })}><option value="">Seleccionar OP…</option>{opciones?.ordenes.map((x) => <option key={x.id} value={x.id}>{x.codigo} · {x.producto}</option>)}</Selector><Selector texto="Lote de crema" valor={datos.crema} cambiar={(v) => setDatos({ ...datos, crema: v, kg: "" })}><option value="">Seleccionar crema…</option>{opciones?.cremas.map((x) => <option key={x.id} value={x.id}>{x.codigo} · disponible {Number(x.disponible_kg).toLocaleString("es-CL")} kg</option>)}</Selector><label className="text-sm font-medium text-slate-700">Crema a utilizar (kg)<input required min="0.001" max={crema?.disponible_kg} step="0.001" type="number" value={datos.kg} onChange={(e) => setDatos({ ...datos, kg: e.target.value })} className={campo} /></label><Selector texto="Línea / equipo" valor={datos.equipo} cambiar={(v) => setDatos({ ...datos, equipo: v })}><option value="">Seleccionar línea…</option>{opciones?.equipos.map((x) => <option key={x.id} value={x.id}>{x.nombre}</option>)}</Selector><label className="text-sm font-medium text-slate-700">Código nuevo lote de mantequilla<input required value={datos.codigo} onChange={(e) => setDatos({ ...datos, codigo: e.target.value.toUpperCase() })} className={campo} /></label><Selector texto="Lote de suero (si se medirá)" valor={datos.suero} cambiar={(v) => setDatos({ ...datos, suero: v })}><option value="">Sin suero declarado</option>{opciones?.sueros.map((x) => <option key={x.id} value={x.id}>{x.codigo} · {x.producto}</option>)}</Selector></div>}{opciones && (opciones.ordenes.length === 0 || opciones.cremas.length === 0) && <p className="mt-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-800">Falta una OP programada de mantequilla o un lote de crema producido con saldo en kg.</p>}{error && <p className="mt-4 rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={onCerrar} className="px-4 py-2 text-sm text-slate-600">Cancelar</button><button disabled={guardando || !datos.orden || !datos.crema || !datos.equipo || !datos.codigo || !datos.kg} className="rounded-xl bg-amber-700 px-5 py-2.5 text-sm font-semibold text-white disabled:opacity-40">{guardando ? "Creando…" : "Crear corrida"}</button></div></form></div>;
}

function Selector({ texto, valor, cambiar, children }: { texto: string; valor: string; cambiar: (valor: string) => void; children: React.ReactNode }) { return <label className="text-sm font-medium text-slate-700">{texto}<select required={texto !== "Lote de suero (si se medirá)"} value={valor} onChange={(e) => cambiar(e.target.value)} className={campo}>{children}</select></label>; }
